# -- Toggle deployment of ECK Operator.
enabled: true
git:
  repo: https://gitlab.com/enbuild-staging/charts/eck-operator.git
  path: "./chart"
  tag: "1.7.1-bb.0"

# -- Flux reconciliation overrides specifically for the ECK Operator Package
flux: {}

# -- Values to passthrough to the eck-operator chart: https://gitlab.com/enbuild-staging/charts/eck-operator.git
values: {}