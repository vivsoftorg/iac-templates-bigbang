addons:
  mattermostoperator:
    enabled: false
    git:
      repo: https://gitlab.com/enbuild-staging/charts/mattermost-operator.git
      path: "./chart"
      tag: "1.18.1-bb.0"

    # -- Flux reconciliation overrides specifically for the Mattermost Operator Package
    flux: {}

    # -- Values to passthrough to the mattermost operator chart: https://gitlab.com/enbuild-staging/charts/mattermost-operator/-/blob/main/chart/values.yaml
    values: {}

    # -- Post Renderers.  See docs/postrenders.md
    postRenderers: []
